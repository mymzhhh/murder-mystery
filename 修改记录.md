# 剧本杀平台 — 修改记录

## 2026-05-10

### 修复：script-reviewer.js 语法错误
- **问题**：编辑模板字符串时，旧内容（评分维度3-5、JSON输出格式）作为孤儿代码残留在模板字面量外部
- **修复**：删除孤儿代码段，将评分维度和输出格式正确整合回模板字面量

### 修复：script-splitter.js 语法错误
- **问题**：NPC角色循环后多了一个闭合括号 `}`，导致 `splitScript` 函数提前关闭，后续 `await` 在非 async 上下文中报错
- **修复**：删除多余括号

### 修复：admin.html JS 语法错误
- **问题**：添加 AbortController 取消按钮时，SSE 事件流的 buf/行解析代码被意外截断，导致 try 块缺少 catch/finally
- **修复**：恢复缺失的 SSE 解析代码（`buf += dec.decode`, `lines.split`, `for` 循环）

### PVE 角色区分系统
- **player 角色**：≤6人，生成完整个人剧本（故事/秘密/时间线/目标/谎言/辩护）
- **NPC 嫌疑人**：≤3人，生成精简信息（故事/秘密/与案件相关信息）
- 总角色数：≤9人
- 覆盖全流水线：
  - **撰写**：prompt 前置角色约束，生成后自动验证+最多3次重试
  - **评测**：硬性约束检查（一票否决制），角色数违规直接20分
  - **切分**：玩家角色→完整纯化剧本，NPC→精简版（≤2000字），存入 split:* 时标记 roleType
  - **进度**：可视化区分 `[玩家]` 和 `[NPC]` 生成进度
  - **模板**：murder-mystery.js 新增角色类型字段、PVE分表要求

### 流水线 UI
- **5步流程**：输入需求 → 提示词优化 → 生成剧本 → 评测 → 切分完成
- **Prompt Agent** (`modules/prompt-agent.js`)：分析用户输入，输出结构化优化prompt，用户可修改确认
- **进度可视化**：玩家剧本(紫色)/NPC剧本(金色)/评测通过(绿色)/失败(红色)
- **取消按钮**：AbortController 中断生成

### 代码结构优化
- `server.js` (612行→37行) 拆分为 `routes/auth.js`、`routes/admin.js`、`routes/player.js`、`socket/game.js`、`socket/ai-dm.js`
- `player.html` (518行→63行) 提取 JS 到 `player.js` (453行)

### Redis 数据规范化
- 只保留切分后数据 `split:{sessionId}:*` 命名空间
- `split:{sessionId}:char:{name}` — 角色(含 roleType)
- `split:{sessionId}:clue:{id}` — 纯化线索(无推理提示)
- `split:{sessionId}:dm` — DM手册
- `split:{sessionId}:meta` — 元数据
- `scripts:split` — 已切分剧本索引

---

## 2026-05-09

### 修复：剧本解析器
- **问题**：`script-parser.js` 无法正确解析已生成剧本的 Markdown 格式
- **修复**：优先匹配 `# 《标题》`、`### 角色X：姓名`、`### A1：线索标题` 等结构化格式，三种方法回退

### 新增功能
- **用户系统** (`modules/auth.js`)：注册/登录/角色管理(PBKDF2+SHA512)
- **AI DM Agent** (`modules/dm-agent.js`)：全自动叙事/线索决策/投票揭示
- **剧本撰写Agent** (`modules/script-writer.js`)：人数检测/查重/防注入约束
- **剧本评测Agent** (`modules/script-reviewer.js`)：5维度评分/不通过自动修改(最多3轮)
- **剧本切分Agent** (`modules/script-splitter.js`)：纯化玩家剧本/线索/DM手册，存入 Redis
- **WebSocket 多人游戏**：Socket.io 房间/角色选择/搜证/投票/真相揭露
- **WebRTC 实时语音**：P2P 网格音频/单人静音/氛围音效
- **常驻聊天框**：游戏各阶段右侧面板
- **沉浸式 UI**：暗黑主题 + 金色点缀 + 粒子背景 + Web Audio 音效 + 翻书动画

### 部署
- Docker Compose (Redis + Node.js)
- Railway 部署 (`murder-mystery-production-8b39.up.railway.app`)
- Redis 端口映射 `6380:6379`（宿主机6379被占用）

### 已知待修复
| 问题 | 优先级 |
|------|--------|
| 第三轮线索解析不完整(2/12条) | 低 |
| Redis 重启后 token 失效 | 低(已加401自动跳转登录) |
